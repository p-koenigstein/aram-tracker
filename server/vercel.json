
{
  "version":2,
  "builds":[
    {
      "src":"index.js",
      "use":"@vercel/node"
    }
  ],
  "build": {
    "command": "npm ci && npm run build"
  },
  "routes":[
    {
      "src":"/(.*)",
      "dest":"/"
    }
  ]
}